package com.focustech.android.components.mt.sdk.android.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table GROUP_SETTING.
 */
public class GroupSettingDao extends AbstractDao<GroupSetting, Long> {

    public static final String TABLENAME = "GROUP_SETTING";

    /**
     * Properties of entity GroupSetting.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property GroupId = new Property(2, String.class, "groupId", false, "GROUP_ID");
        public final static Property MessageSetting = new Property(3, long.class, "messageSetting", false, "MESSAGE_SETTING");
        public final static Property Setting = new Property(4, String.class, "setting", false, "SETTING");
        public final static Property Feature = new Property(5, long.class, "feature", false, "FEATURE");
    }

    ;


    public GroupSettingDao(DaoConfig config) {
        super(config);
    }

    public GroupSettingDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'GROUP_SETTING' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'USER_ID' TEXT NOT NULL ," + // 1: userId
                "'GROUP_ID' TEXT NOT NULL ," + // 2: groupId
                "'MESSAGE_SETTING' INTEGER NOT NULL ," + // 3: messageSetting
                "'SETTING' TEXT," + // 4: setting
                "'FEATURE' INTEGER NOT NULL );"); // 5: feature
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'GROUP_SETTING'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GroupSetting entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getUserId());
        stmt.bindString(3, entity.getGroupId());
        stmt.bindLong(4, entity.getMessageSetting());
 
        String setting = entity.getSetting();
        if (setting != null) {
            stmt.bindString(5, setting);
        }
        stmt.bindLong(6, entity.getFeature());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public GroupSetting readEntity(Cursor cursor, int offset) {
        GroupSetting entity = new GroupSetting( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
                cursor.getString(offset + 1), // userId
                cursor.getString(offset + 2), // groupId
                cursor.getLong(offset + 3), // messageSetting
                cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // setting
                cursor.getLong(offset + 5) // feature
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GroupSetting entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.getString(offset + 1));
        entity.setGroupId(cursor.getString(offset + 2));
        entity.setMessageSetting(cursor.getLong(offset + 3));
        entity.setSetting(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setFeature(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(GroupSetting entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(GroupSetting entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
