package com.focustech.android.components.mt.sdk.android.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table CONVERSATION.
 */
public class ConversationDao extends AbstractDao<Conversation, Long> {

    public static final String TABLENAME = "CONVERSATION";

    /**
     * Properties of entity Conversation.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property ContactType = new Property(2, long.class, "contactType", false, "CONTACT_TYPE");
        public final static Property ContactId = new Property(3, String.class, "contactId", false, "CONTACT_ID");
        public final static Property ContactName = new Property(4, String.class, "contactName", false, "CONTACT_NAME");
        public final static Property Setting = new Property(5, String.class, "setting", false, "SETTING");
        public final static Property Timestamp = new Property(6, long.class, "timestamp", false, "TIMESTAMP");
    }

    ;


    public ConversationDao(DaoConfig config) {
        super(config);
    }

    public ConversationDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CONVERSATION' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'USER_ID' TEXT NOT NULL ," + // 1: userId
                "'CONTACT_TYPE' INTEGER NOT NULL ," + // 2: contactType
                "'CONTACT_ID' TEXT NOT NULL ," + // 3: contactId
                "'CONTACT_NAME' TEXT," + // 4: contactName
                "'SETTING' TEXT," + // 5: setting
                "'TIMESTAMP' INTEGER NOT NULL );"); // 6: timestamp
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CONVERSATION'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Conversation entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getUserId());
        stmt.bindLong(3, entity.getContactType());
        stmt.bindString(4, entity.getContactId());
 
        String contactName = entity.getContactName();
        if (contactName != null) {
            stmt.bindString(5, contactName);
        }
 
        String setting = entity.getSetting();
        if (setting != null) {
            stmt.bindString(6, setting);
        }
        stmt.bindLong(7, entity.getTimestamp());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Conversation readEntity(Cursor cursor, int offset) {
        Conversation entity = new Conversation( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
                cursor.getString(offset + 1), // userId
                cursor.getLong(offset + 2), // contactType
                cursor.getString(offset + 3), // contactId
                cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // contactName
                cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // setting
                cursor.getLong(offset + 6) // timestamp
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Conversation entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.getString(offset + 1));
        entity.setContactType(cursor.getLong(offset + 2));
        entity.setContactId(cursor.getString(offset + 3));
        entity.setContactName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setSetting(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setTimestamp(cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Conversation entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Conversation entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
