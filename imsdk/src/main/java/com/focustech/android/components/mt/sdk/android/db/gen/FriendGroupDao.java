package com.focustech.android.components.mt.sdk.android.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table FRIEND_GROUP.
 */
public class FriendGroupDao extends AbstractDao<FriendGroup, Long> {

    public static final String TABLENAME = "FRIEND_GROUP";

    /**
     * Properties of entity FriendGroup.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, String.class, "userId", false, "USER_ID");
        public final static Property FriendGroupId = new Property(2, String.class, "friendGroupId", false, "FRIEND_GROUP_ID");
        public final static Property FriendGroupName = new Property(3, String.class, "friendGroupName", false, "FRIEND_GROUP_NAME");
        public final static Property FriendGroupType = new Property(4, long.class, "friendGroupType", false, "FRIEND_GROUP_TYPE");
    }

    ;


    public FriendGroupDao(DaoConfig config) {
        super(config);
    }

    public FriendGroupDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'FRIEND_GROUP' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'USER_ID' TEXT NOT NULL ," + // 1: userId
                "'FRIEND_GROUP_ID' TEXT NOT NULL ," + // 2: friendGroupId
                "'FRIEND_GROUP_NAME' TEXT NOT NULL ," + // 3: friendGroupName
                "'FRIEND_GROUP_TYPE' INTEGER NOT NULL );"); // 4: friendGroupType
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'FRIEND_GROUP'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, FriendGroup entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getUserId());
        stmt.bindString(3, entity.getFriendGroupId());
        stmt.bindString(4, entity.getFriendGroupName());
        stmt.bindLong(5, entity.getFriendGroupType());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public FriendGroup readEntity(Cursor cursor, int offset) {
        FriendGroup entity = new FriendGroup( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
                cursor.getString(offset + 1), // userId
                cursor.getString(offset + 2), // friendGroupId
                cursor.getString(offset + 3), // friendGroupName
                cursor.getLong(offset + 4) // friendGroupType
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, FriendGroup entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.getString(offset + 1));
        entity.setFriendGroupId(cursor.getString(offset + 2));
        entity.setFriendGroupName(cursor.getString(offset + 3));
        entity.setFriendGroupType(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(FriendGroup entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(FriendGroup entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
